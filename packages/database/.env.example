# Woof Database Configuration
# Copy this to .env and update with your credentials

DATABASE_URL="postgresql://postgres:password@localhost:5432/woof?schema=public"

# For pgvector support:
# Make sure your PostgreSQL instance has the pgvector extension installed
# CREATE EXTENSION IF NOT EXISTS vector;
