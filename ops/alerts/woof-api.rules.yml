# GENERATED by .github/scripts/woof-alert-policy.py from woof-api-alert-policy.v1.json.
# Do not edit thresholds here. Change the policy JSON and regenerate.
groups:
  - name: woof-api-operational-alerts-v1
    interval: 30s
    rules:
    - alert: WoofOperationalMetricsMissingWarning
      expr: >-
        absent(woof_process_uptime_seconds{service="woof-api"})
      for: 5m
      labels:
        severity: "warning"
        service: "woof-api"
        operation_class: "telemetry"
        policy_version: "woof-api-alert-policy-v1"
        release: "unknown"
      annotations:
        summary: "Woof API operational metrics are missing (warning)"
        description: "The API metrics heartbeat is absent. Missing telemetry is an observability incident, not evidence of health."
    - alert: WoofOperationalMetricsMissingCritical
      expr: >-
        absent(woof_process_uptime_seconds{service="woof-api"})
      for: 15m
      labels:
        severity: "critical"
        service: "woof-api"
        operation_class: "telemetry"
        policy_version: "woof-api-alert-policy-v1"
        release: "unknown"
      annotations:
        summary: "Woof API operational metrics are missing (critical)"
        description: "The API metrics heartbeat is absent. Missing telemetry is an observability incident, not evidence of health."
    - alert: WoofReleaseIdentityUnknownWarning
      expr: >-
        woof_release_info{service="woof-api",release="unknown"} == 1
      for: 5m
      labels:
        severity: "warning"
        service: "woof-api"
        operation_class: "release_identity"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof API is exporting an unknown release identity"
        description: "Operational evidence is not tied to an exact 40-hex Git SHA; do not promote this process as a qualified release."
    - alert: WoofReadinessProbeMissingWarning
      expr: >-
        max by (release) (woof_release_info{service="woof-api"})
        unless on (release)
        count by (release) (woof_http_requests_total{service="woof-api",operation="ObservabilityController.readiness"})
      for: 5m
      labels:
        severity: "warning"
        service: "woof-api"
        operation_class: "readiness"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof API readiness probe telemetry is missing (warning)"
        description: "A scraped Woof release has no readiness-probe series. Verify external probing before treating that release as healthy."
    - alert: WoofReadinessProbeMissingCritical
      expr: >-
        max by (release) (woof_release_info{service="woof-api"})
        unless on (release)
        count by (release) (woof_http_requests_total{service="woof-api",operation="ObservabilityController.readiness"})
      for: 15m
      labels:
        severity: "critical"
        service: "woof-api"
        operation_class: "readiness"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof API readiness probe telemetry is missing (critical)"
        description: "A scraped Woof release has no readiness-probe series. Verify external probing before treating that release as healthy."
    - alert: WoofReadinessFailuresWarning
      expr: >-
        sum by (release) (increase(woof_http_requests_total{service="woof-api",operation="ObservabilityController.readiness",status_class="5xx"}[5m])) >= 1
      for: 1m
      labels:
        severity: "warning"
        service: "woof-api"
        operation_class: "readiness"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof API readiness is failing (warning)"
        description: "Database-backed readiness has returned server errors within the bounded alert window for release {{ $labels.release }}."
    - alert: WoofReadinessFailuresCritical
      expr: >-
        sum by (release) (increase(woof_http_requests_total{service="woof-api",operation="ObservabilityController.readiness",status_class="5xx"}[5m])) >= 3
      for: 1m
      labels:
        severity: "critical"
        service: "woof-api"
        operation_class: "readiness"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof API readiness is failing (critical)"
        description: "Database-backed readiness has returned server errors within the bounded alert window for release {{ $labels.release }}."
    - alert: WoofHttp5xxRatioWarning
      expr: >-
        (
          sum by (release) (rate(woof_http_requests_total{service="woof-api",operation!~"ObservabilityController[.]liveness|ObservabilityController[.]readiness|ObservabilityController[.]prometheus|ObservabilityController[.]snapshot",status_class="5xx"}[5m]))
          /
          clamp_min(sum by (release) (rate(woof_http_requests_total{service="woof-api",operation!~"ObservabilityController[.]liveness|ObservabilityController[.]readiness|ObservabilityController[.]prometheus|ObservabilityController[.]snapshot",status_class=~"2xx|3xx|5xx"}[5m])), 0.000001)
        ) >= 0.02
        and on (release)
        sum by (release) (increase(woof_http_requests_total{service="woof-api",operation!~"ObservabilityController[.]liveness|ObservabilityController[.]readiness|ObservabilityController[.]prometheus|ObservabilityController[.]snapshot",status_class=~"2xx|3xx|5xx"}[5m])) >= 50
      for: 5m
      labels:
        severity: "warning"
        service: "woof-api"
        operation_class: "http_service"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof API application 5xx ratio is elevated (warning)"
        description: "The bounded application-request 5xx ratio exceeded policy for release {{ $labels.release }} after the minimum eligible-request floor was met. Health/metrics endpoints and 4xx client outcomes are excluded from this ratio."
    - alert: WoofHttp5xxRatioCritical
      expr: >-
        (
          sum by (release) (rate(woof_http_requests_total{service="woof-api",operation!~"ObservabilityController[.]liveness|ObservabilityController[.]readiness|ObservabilityController[.]prometheus|ObservabilityController[.]snapshot",status_class="5xx"}[5m]))
          /
          clamp_min(sum by (release) (rate(woof_http_requests_total{service="woof-api",operation!~"ObservabilityController[.]liveness|ObservabilityController[.]readiness|ObservabilityController[.]prometheus|ObservabilityController[.]snapshot",status_class=~"2xx|3xx|5xx"}[5m])), 0.000001)
        ) >= 0.05
        and on (release)
        sum by (release) (increase(woof_http_requests_total{service="woof-api",operation!~"ObservabilityController[.]liveness|ObservabilityController[.]readiness|ObservabilityController[.]prometheus|ObservabilityController[.]snapshot",status_class=~"2xx|3xx|5xx"}[5m])) >= 50
      for: 2m
      labels:
        severity: "critical"
        service: "woof-api"
        operation_class: "http_service"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof API application 5xx ratio is elevated (critical)"
        description: "The bounded application-request 5xx ratio exceeded policy for release {{ $labels.release }} after the minimum eligible-request floor was met. Health/metrics endpoints and 4xx client outcomes are excluded from this ratio."
    - alert: WoofApplication5xxBurstWarning
      expr: >-
        sum by (release, operation) (increase(woof_http_requests_total{service="woof-api",operation!~"ObservabilityController[.]liveness|ObservabilityController[.]readiness|ObservabilityController[.]prometheus|ObservabilityController[.]snapshot",status_class="5xx"}[15m])) >= 3
      for: 5m
      labels:
        severity: "warning"
        service: "woof-api"
        operation_class: "http_operation_burst"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof API operation has repeated 5xx responses (warning)"
        description: "Application operation {{ $labels.operation }} has repeated server errors for release {{ $labels.release }}. This low-volume fallback remains active even when ratio sample floors are not met."
    - alert: WoofApplication5xxBurstCritical
      expr: >-
        sum by (release, operation) (increase(woof_http_requests_total{service="woof-api",operation!~"ObservabilityController[.]liveness|ObservabilityController[.]readiness|ObservabilityController[.]prometheus|ObservabilityController[.]snapshot",status_class="5xx"}[15m])) >= 10
      for: 2m
      labels:
        severity: "critical"
        service: "woof-api"
        operation_class: "http_operation_burst"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof API operation has repeated 5xx responses (critical)"
        description: "Application operation {{ $labels.operation }} has repeated server errors for release {{ $labels.release }}. This low-volume fallback remains active even when ratio sample floors are not met."
    - alert: WoofAuth5xxRatioWarning
      expr: >-
        (
          sum by (release, operation) (rate(woof_http_requests_total{service="woof-api",operation=~"AuthController[.]register|AuthController[.]login|AuthController[.]logout|AuthController[.]logoutAll|AuthController[.]getProfile",status_class="5xx"}[5m]))
          /
          clamp_min(sum by (release, operation) (rate(woof_http_requests_total{service="woof-api",operation=~"AuthController[.]register|AuthController[.]login|AuthController[.]logout|AuthController[.]logoutAll|AuthController[.]getProfile",status_class=~"2xx|3xx|5xx"}[5m])), 0.000001)
        ) >= 0.02
        and on (release, operation)
        sum by (release, operation) (increase(woof_http_requests_total{service="woof-api",operation=~"AuthController[.]register|AuthController[.]login|AuthController[.]logout|AuthController[.]logoutAll|AuthController[.]getProfile",status_class=~"2xx|3xx|5xx"}[5m])) >= 50
      for: 5m
      labels:
        severity: "warning"
        service: "woof-api"
        operation_class: "auth_session"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof authentication/session operation 5xx ratio is elevated (warning)"
        description: "Auth/session operation {{ $labels.operation }} is returning server errors above policy for release {{ $labels.release }}. Expected 4xx authentication denials are excluded from both numerator and denominator."
    - alert: WoofAuth5xxRatioCritical
      expr: >-
        (
          sum by (release, operation) (rate(woof_http_requests_total{service="woof-api",operation=~"AuthController[.]register|AuthController[.]login|AuthController[.]logout|AuthController[.]logoutAll|AuthController[.]getProfile",status_class="5xx"}[5m]))
          /
          clamp_min(sum by (release, operation) (rate(woof_http_requests_total{service="woof-api",operation=~"AuthController[.]register|AuthController[.]login|AuthController[.]logout|AuthController[.]logoutAll|AuthController[.]getProfile",status_class=~"2xx|3xx|5xx"}[5m])), 0.000001)
        ) >= 0.05
        and on (release, operation)
        sum by (release, operation) (increase(woof_http_requests_total{service="woof-api",operation=~"AuthController[.]register|AuthController[.]login|AuthController[.]logout|AuthController[.]logoutAll|AuthController[.]getProfile",status_class=~"2xx|3xx|5xx"}[5m])) >= 50
      for: 2m
      labels:
        severity: "critical"
        service: "woof-api"
        operation_class: "auth_session"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof authentication/session operation 5xx ratio is elevated (critical)"
        description: "Auth/session operation {{ $labels.operation }} is returning server errors above policy for release {{ $labels.release }}. Expected 4xx authentication denials are excluded from both numerator and denominator."
    - alert: WoofTodayReadP95LatencyWarning
      expr: >-
        (
          histogram_quantile(0.95,
            sum by (le, release, operation) (
              rate(woof_http_request_duration_ms_bucket{service="woof-api",operation=~"AdventureController[.]getMine|CompanionController[.]getState|CompanionController[.]getReadiness|CaregiverController[.]getCaregiverToday",status_class="2xx"}[10m])
            )
          )
        ) >= 750
        and on (release, operation)
        sum by (release, operation) (increase(woof_http_request_duration_ms_count{service="woof-api",operation=~"AdventureController[.]getMine|CompanionController[.]getState|CompanionController[.]getReadiness|CaregiverController[.]getCaregiverToday",status_class="2xx"}[10m])) >= 20
      for: 10m
      labels:
        severity: "warning"
        service: "woof-api"
        operation_class: "today_read"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof Today/read operation p95 latency is elevated (warning)"
        description: "Histogram-derived successful-response p95 handler latency for {{ $labels.operation }} exceeded the initial operator guardrail for release {{ $labels.release }} after the minimum sample floor was met."
    - alert: WoofTodayReadP95LatencyCritical
      expr: >-
        (
          histogram_quantile(0.95,
            sum by (le, release, operation) (
              rate(woof_http_request_duration_ms_bucket{service="woof-api",operation=~"AdventureController[.]getMine|CompanionController[.]getState|CompanionController[.]getReadiness|CaregiverController[.]getCaregiverToday",status_class="2xx"}[10m])
            )
          )
        ) >= 1500
        and on (release, operation)
        sum by (release, operation) (increase(woof_http_request_duration_ms_count{service="woof-api",operation=~"AdventureController[.]getMine|CompanionController[.]getState|CompanionController[.]getReadiness|CaregiverController[.]getCaregiverToday",status_class="2xx"}[10m])) >= 20
      for: 5m
      labels:
        severity: "critical"
        service: "woof-api"
        operation_class: "today_read"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof Today/read operation p95 latency is elevated (critical)"
        description: "Histogram-derived successful-response p95 handler latency for {{ $labels.operation }} exceeded the initial operator guardrail for release {{ $labels.release }} after the minimum sample floor was met."
    - alert: WoofRequestDurationTelemetryInvalidWarning
      expr: >-
        sum by (release, operation) (increase(woof_http_request_duration_invalid_total{service="woof-api"}[15m])) >= 1
      for: 5m
      labels:
        severity: "warning"
        service: "woof-api"
        operation_class: "telemetry_quality"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof request-duration telemetry is invalid (warning)"
        description: "Request timing for {{ $labels.operation }} produced non-finite or negative samples for release {{ $labels.release }}. Invalid samples are excluded from latency histograms rather than recorded as fake zeroes."
    - alert: WoofRequestDurationTelemetryInvalidCritical
      expr: >-
        sum by (release, operation) (increase(woof_http_request_duration_invalid_total{service="woof-api"}[15m])) >= 5
      for: 2m
      labels:
        severity: "critical"
        service: "woof-api"
        operation_class: "telemetry_quality"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof request-duration telemetry is invalid (critical)"
        description: "Request timing for {{ $labels.operation }} produced non-finite or negative samples for release {{ $labels.release }}. Invalid samples are excluded from latency histograms rather than recorded as fake zeroes."
    - alert: WoofCaregiverTransition5xxRatioWarning
      expr: >-
        (
          sum by (release, operation) (rate(woof_http_requests_total{service="woof-api",operation=~"CaregiverController[.]issueGrant|CaregiverController[.]acceptGrant|CaregiverController[.]declineGrant|CaregiverController[.]revokeGrant",status_class="5xx"}[5m]))
          /
          clamp_min(sum by (release, operation) (rate(woof_http_requests_total{service="woof-api",operation=~"CaregiverController[.]issueGrant|CaregiverController[.]acceptGrant|CaregiverController[.]declineGrant|CaregiverController[.]revokeGrant",status_class=~"2xx|3xx|5xx"}[5m])), 0.000001)
        ) >= 0.02
        and on (release, operation)
        sum by (release, operation) (increase(woof_http_requests_total{service="woof-api",operation=~"CaregiverController[.]issueGrant|CaregiverController[.]acceptGrant|CaregiverController[.]declineGrant|CaregiverController[.]revokeGrant",status_class=~"2xx|3xx|5xx"}[5m])) >= 25
      for: 5m
      labels:
        severity: "warning"
        service: "woof-api"
        operation_class: "caregiver_transition"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof caregiver transition operation 5xx ratio is elevated (warning)"
        description: "Pet-scoped caregiver operation {{ $labels.operation }} is returning server errors above policy for release {{ $labels.release }}. Client/authorization 4xx outcomes do not dilute the server-error ratio."
    - alert: WoofCaregiverTransition5xxRatioCritical
      expr: >-
        (
          sum by (release, operation) (rate(woof_http_requests_total{service="woof-api",operation=~"CaregiverController[.]issueGrant|CaregiverController[.]acceptGrant|CaregiverController[.]declineGrant|CaregiverController[.]revokeGrant",status_class="5xx"}[5m]))
          /
          clamp_min(sum by (release, operation) (rate(woof_http_requests_total{service="woof-api",operation=~"CaregiverController[.]issueGrant|CaregiverController[.]acceptGrant|CaregiverController[.]declineGrant|CaregiverController[.]revokeGrant",status_class=~"2xx|3xx|5xx"}[5m])), 0.000001)
        ) >= 0.05
        and on (release, operation)
        sum by (release, operation) (increase(woof_http_requests_total{service="woof-api",operation=~"CaregiverController[.]issueGrant|CaregiverController[.]acceptGrant|CaregiverController[.]declineGrant|CaregiverController[.]revokeGrant",status_class=~"2xx|3xx|5xx"}[5m])) >= 25
      for: 2m
      labels:
        severity: "critical"
        service: "woof-api"
        operation_class: "caregiver_transition"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof caregiver transition operation 5xx ratio is elevated (critical)"
        description: "Pet-scoped caregiver operation {{ $labels.operation }} is returning server errors above policy for release {{ $labels.release }}. Client/authorization 4xx outcomes do not dilute the server-error ratio."
    - alert: WoofConnectorRejectedRatioWarning
      expr: >-
        (
          sum by (release, provider, kind) (rate(woof_connector_imports_total{service="woof-api",outcome="REJECTED"}[15m]))
          /
          clamp_min(sum by (release, provider, kind) (rate(woof_connector_imports_total{service="woof-api"}[15m])), 0.000001)
        ) >= 0.1
        and on (release, provider, kind)
        sum by (release, provider, kind) (increase(woof_connector_imports_total{service="woof-api"}[15m])) >= 20
      for: 10m
      labels:
        severity: "warning"
        service: "woof-api"
        operation_class: "connector_import"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof connector rejection ratio is elevated (warning)"
        description: "Verified {{ $labels.provider }} {{ $labels.kind }} imports are being rejected above the initial operator guardrail for release {{ $labels.release }}."
    - alert: WoofConnectorRejectedRatioCritical
      expr: >-
        (
          sum by (release, provider, kind) (rate(woof_connector_imports_total{service="woof-api",outcome="REJECTED"}[15m]))
          /
          clamp_min(sum by (release, provider, kind) (rate(woof_connector_imports_total{service="woof-api"}[15m])), 0.000001)
        ) >= 0.25
        and on (release, provider, kind)
        sum by (release, provider, kind) (increase(woof_connector_imports_total{service="woof-api"}[15m])) >= 20
      for: 5m
      labels:
        severity: "critical"
        service: "woof-api"
        operation_class: "connector_import"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof connector rejection ratio is elevated (critical)"
        description: "Verified {{ $labels.provider }} {{ $labels.kind }} imports are being rejected above the initial operator guardrail for release {{ $labels.release }}."
    - alert: WoofDeviceContractRejectionsWarning
      expr: >-
        sum by (release) (increase(woof_device_contract_rejections_total{service="woof-api"}[15m])) >= 5
      for: 10m
      labels:
        severity: "warning"
        service: "woof-api"
        operation_class: "device_contract"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof device-contract rejections are elevated (warning)"
        description: "Untrusted device envelopes are being rejected above policy for release {{ $labels.release }}. Inspect partner transport without logging raw payloads."
    - alert: WoofDeviceContractRejectionsCritical
      expr: >-
        sum by (release) (increase(woof_device_contract_rejections_total{service="woof-api"}[15m])) >= 20
      for: 5m
      labels:
        severity: "critical"
        service: "woof-api"
        operation_class: "device_contract"
        policy_version: "woof-api-alert-policy-v1"
      annotations:
        summary: "Woof device-contract rejections are elevated (critical)"
        description: "Untrusted device envelopes are being rejected above policy for release {{ $labels.release }}. Inspect partner transport without logging raw payloads."
